import { PgslShader } from '../structure/pgsl-shader';

/**
 * PGSL Code interpreter.
 */
export class PgslInterpreter {

    public constructor(pDevice: GpuDevice) {
        // TODO: Init and build pgsl code parser.
    }

    public convert(pSource: string): PgslShader {
        // TODO: Convert shader based on constructed code parser.
    }
}