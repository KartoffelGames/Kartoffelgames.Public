export enum PgslBaseTypeName {
    Numberic = '*Numeric*', // Should never be writen
    Boolean = 'Boolean',
    Vector = '*Vector*', // Should never be writen
    Matrix = '*Matrix*', // Should never be writen
    String = 'String',
    Struct = '*Struct*', // Should never be writen
    Texture = '*Texture*', // Should never be writen
    Sampler = '*Sampler*', // Should never be writen
    Array = 'Array',
    Pointer = '*Pointer*', // Should never be writen
    BuildIn = '*BuildIn*', // Should never be writen
    Void = 'Void',
    Alias = '*Alias*', // Should never be writen
    Enum = '*Enum*' // Should never be writen
}