declare module '*.wgsl' { 
    /* LOADER::raw-loader */
    const Content: string;
    export default Content;
}

declare module '*.pgsl' { 
    /* LOADER::raw-loader */
    const Content: string;
    export default Content;
}